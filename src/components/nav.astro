<nav class="navbar">
  <div class="navbar-left">
    <img
      src="https://media3.giphy.com/media/v1.Y2lkPTc5MGI3NjExNTVyaWx6YWZmNWhmZnhpaGthMzlhbzd0dzFxdTB4YzcwZTY0OHRkNiZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/74augs47dylVNAPIEt/giphy.gif"
      alt="Aoni Design Logo"
      class="logo"
    />
    <div class="brand">
      <span class="brand-title">Aoni Design</span>
      <!-- <img src="/plus.svg" alt="Aoni Design mini" class="brand-mini" /> -->
    </div>
  </div>
  <ul class="navbar-center" id="navbar-center">
    <li><a href="#" class="nav-link">Inicio</a></li>
    <li><a href="#" class="nav-link">Nosotros</a></li>
    <li><a href="#" class="nav-link">Servicios</a></li>
    <li><a href="#" class="nav-link">Works</a></li>
    <li class="contact-nav-center">
      <a href="#" class="contact-btn contact-btn-center"
        ><span class="dot-green"></span>Contactanos</a
      >
    </li>
  </ul>
  <div class="navbar-right">
    <a href="#" class="contact-btn contact-btn-right">
      <span class="dot-green"></span>Contactanos
    </a>
  </div>
  <button class="navbar-toggle" id="navbar-toggle" aria-label="Abrir menú">
    <span></span><span></span><span></span>
  </button>
</nav>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap");
  .navbar {
    width: 100vw;
    max-width: 100vw;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.8rem 1.5rem;
    box-sizing: border-box;
    font-family: "Poppins", sans-serif;
    min-height: 56px;
    position: fixed;
    top: 0;
    left: 0;
    z-index: 2000;
    /*   border: 1px solid rgba(255, 255, 255, 0.13);
    border-radius: 22px;
    background: rgba(20, 20, 20, 0.45);
    backdrop-filter: blur(12px);
    box-shadow: 0 4px 32px 0 rgba(0, 0, 0, 0.12); */
    /* Elimina cualquier fondo sólido anterior */
  }
  .navbar-left {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .logo {
    width: 38px;
    height: 38px;
    border-radius: 50%;
    padding: 2px;
  }
  .brand {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 2px;
  }
  .brand-title {
    color: #fff;
    font-size: 1.1rem;
    font-weight: 500;
    line-height: 1;
  }
  .brand-mini {
    width: 60px;
    height: 18px;
    margin-top: 2px;
    object-fit: contain;
  }
  .navbar-center {
    display: flex;
    align-items: center;
    gap: 2.5rem;

    background: rgba(48, 48, 48, 0.27);
    border-radius: 16px;
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
    backdrop-filter: blur(5px);
    -webkit-backdrop-filter: blur(5px);
    border: 1px solid rgba(52, 52, 52, 0.3);
    border-radius: 12px;
    padding: 0.2rem 2.5rem;
    margin: 0 1.5rem;
    list-style: none;
    min-width: 350px;
    justify-content: center;
    transition: all 0.25s;
  }
  .navbar-center li a {
    color: #fff;
    text-decoration: none;
    font-size: 1.05rem;
    font-weight: 400;
    transition: color 0.2s;
    padding: 0.8rem 0.7rem;
    border-radius: 6px;
    display: block;
  }
  .navbar-center li a:hover {
    color: #ff6a3d;
    background: #232326;
  }
  .navbar-right {
    display: flex;
    align-items: center;
  }
  .contact-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: #232326;
    color: #fff;
    font-weight: 400;
    border-radius: 8px;
    padding: 0.3rem 1.1rem 0.3rem 0.7rem;
    text-decoration: none;
    font-size: 1.05rem;
    transition:
      background 0.2s,
      color 0.2s;
    border: none;
    outline: none;
  }
  .contact-btn:hover {
    background: #2e2e32;
    color: #ff6a3d;
  }
  .dot-green {
    width: 12px;
    height: 12px;
    background: #00ff47;
    border-radius: 50%;
    display: inline-block;
    margin-right: 2px;
    box-shadow: 0 0 6px #00ff47cc;
  }
  .navbar-toggle {
    display: none;
    flex-direction: column;
    gap: 4px;
    background: none;
    border: none;
    cursor: pointer;
    margin-left: 1rem;
    z-index: 1003;
  }
  .navbar-toggle span {
    width: 24px;
    height: 3px;
    background: #fff;
    border-radius: 2px;
    display: block;
  }
  @media (max-width: 900px) {
    .navbar-center {
      gap: 1.2rem;
      padding: 0.2rem 1rem;
      min-width: unset;
    }
    .brand-mini {
      width: 48px;
    }
  }
  @media (max-width: 700px) {
    .navbar {
      flex-direction: row;
      flex-wrap: nowrap;
      padding: 0.7rem 0.7rem;
      border: 1px solid #888888;
      border-radius: 22px;
      /* Mantener glassmorfismo en mobile */
      background: rgba(20, 20, 20, 0.45);
      backdrop-filter: blur(12px);
      box-shadow: 0 4px 32px 0 rgba(0, 0, 0, 0.12);
      min-height: 64px;
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      z-index: 2000;
      box-sizing: border-box;
    }
    .navbar-left {
      flex: 0 0 auto;
      z-index: 2;
    }
    .navbar-right {
      flex: 0 0 auto;
      margin-left: 0;
      z-index: 2;
    }
    .contact-btn {
      position: absolute;
      left: 50%;
      top: 50%;
      transform: translate(-50%, -50%);
      z-index: 1;
      background: #18181b;
      padding: 0.5rem 1.5rem;
      border-radius: 16px;
      font-size: 1.08rem;
      box-shadow: 0 0 0 2px #232326;
      border: none;
    }
    .navbar-center {
      display: none !important;
    }
    .navbar-toggle {
      display: flex;
      z-index: 2;
    }
    .brand-title {
      display: none;
    }
  }
  /* Por defecto, el botón en navbar-center está oculto */
  .contact-nav-center {
    display: none;
  }
  /* En escritorio, cuando la navbar tiene .scrolled, mostrar el botón en navbar-center y ocultar el de la derecha */
  @media (min-width: 701px) {
    .navbar.scrolled .brand {
      display: none !important;
    }
    .navbar.scrolled .contact-nav-center {
      display: list-item;
    }
    .navbar.scrolled .contact-btn-right {
      display: none;
    }
  }
</style>
<script>
  // Script seguro para Astro/hidratación
  if (typeof window !== "undefined") {
    const toggle = document.getElementById("navbar-toggle");
    const links = document.getElementById("navbar-center");
    const navLinks = document.querySelectorAll(".nav-link");
    const navbar = document.querySelector(".navbar");
    if (toggle && links) {
      toggle.addEventListener("click", () => {
        links.classList.toggle("active");
      });
      navLinks.forEach((link) => {
        link.addEventListener("click", () => {
          if (window.innerWidth <= 700) {
            links.classList.remove("active");
          }
        });
      });
    }
    // Scroll: solo en escritorio
    if (navbar) {
      window.addEventListener("scroll", () => {
        if (window.innerWidth > 700) {
          if (window.scrollY > 10) {
            navbar.classList.add("scrolled");
          } else {
            navbar.classList.remove("scrolled");
          }
        } else {
          navbar.classList.remove("scrolled");
        }
      });
    }
  }
</script>
